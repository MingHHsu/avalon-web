import styled from 'styled-components';
import {
  Button,
  Input,
} from 'components/common';

export const CreateRoomRoot = styled.div``;

export const RoomNameInput = styled(Input)``;

export const CreateRoomButton = styled(Button)`
`;
