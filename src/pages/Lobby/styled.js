import styled from 'styled-components';
import {
  Button,
} from 'components/common';

export const LobbyRoot = styled.div`
`;

export const RoomList = styled.div`
`;

export const EnterRoomButton = styled(Button)`
`;
